{
  "name": "property-os-suite",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "db:generate": "pnpm -C prisma db:generate",
    "db:migrate": "pnpm -C prisma db:migrate",
    "db:studio": "pnpm -C prisma db:studio"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
